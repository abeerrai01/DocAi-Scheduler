services:
  - type: web
    name: doc-ai-scheduler-ml
    env: python
    buildCommand: |
      python -m venv venv
      . venv/bin/activate
      pip install -r requirements.txt
    startCommand: |
      . venv/bin/activate
      python model.py
    envVars:
      - key: PORT
        value: 5050 